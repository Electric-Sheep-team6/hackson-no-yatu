import type { Movie } from "@/app/types/last-screen";

export function MovieSection({ movie, hasMedia }: { movie: Movie | null; hasMedia: boolean }) {
  const status = !movie ? "生成前" : movie.status === "completed" ? "映画の構成を受け取りました。" : movie.status === "failed" ? movie.errorMessage ?? "映画の生成に失敗しました。" : `映画を生成しています（${movie.status}）。`;
  return <section className="mt-10 rounded-[32px] border border-white/10 bg-slate-900/80 p-6"><div className="mb-6 flex items-center justify-between"><div><p className="text-xs uppercase tracking-[0.28em] text-slate-400">movie generation</p><h3 className="mt-2 text-2xl font-semibold text-white">最後の映画の予告編</h3></div><span className="text-xs text-pink-200">{hasMedia ? "アップロード済み素材を使用" : "素材待機中"}</span></div><div className="grid gap-6 xl:grid-cols-[1.1fr_0.9fr]"><div className="rounded-[28px] border border-white/10 bg-gradient-to-br from-[#120d1d] to-[#0b1720] p-5"><div className="aspect-video rounded-[20px] border border-white/10 bg-slate-950 p-5"><div className="flex h-full flex-col justify-end"><p className="text-xs uppercase tracking-[0.25em] text-cyan-200">{hasMedia ? "material-driven" : "waiting for upload"}</p><p className="mt-2 text-xl font-semibold text-white">{movie?.movie?.title ?? "写真と動画をアップロードすると、ここに予告編が生まれます"}</p></div></div><div className="mt-5 rounded-2xl border border-cyan-400/30 bg-cyan-500/10 p-4 text-sm text-cyan-100">{status}</div></div><div className="rounded-2xl border border-white/10 bg-white/5 p-4"><p className="text-xs uppercase tracking-[0.22em] text-slate-400">章構成</p><ul className="mt-4 space-y-3">{movie?.movie?.scenes.map((scene) => <li key={scene.order} className="rounded-xl border border-white/10 bg-slate-950/50 px-3 py-2"><p className="text-sm font-medium text-white">{scene.source}</p><p className="mt-1 text-xs text-slate-300">{scene.narration}</p></li>) ?? <li className="text-sm text-slate-400">生成後に章構成が表示されます。</li>}</ul></div></div></section>;
}
